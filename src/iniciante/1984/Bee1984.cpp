#include <algorithm>
#include <bits/stdc++.h>
#include <string>
using namespace std;
int main(int argc, char *argv[]) {
  string str;
  cin >> str;
  reverse(str.begin(), str.end());
  cout << str << endl;
  return 0;
}
